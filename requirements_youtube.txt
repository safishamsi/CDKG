# Additional requirements for YouTube integration

# YouTube downloader
yt-dlp>=2024.1.0

# Audio transcription
openai-whisper>=20231117
# Note: ffmpeg must be installed separately on your system
# macOS: brew install ffmpeg
# Ubuntu/Debian: sudo apt-get install ffmpeg
# Windows: Download from https://ffmpeg.org/download.html

# Existing requirements (keep these)
neo4j>=5.15.0
python-dotenv>=1.0.0
pandas>=2.0.0
sentence-transformers>=2.2.2
transformers>=4.30.0
torch>=2.0.0
faiss-cpu>=1.7.4
numpy>=1.24.0
anthropic>=0.25.0
tqdm>=4.65.0
pydantic>=2.0.0

# API
fastapi>=0.104.0
uvicorn>=0.24.0

# LangGraph (if using)
langgraph>=0.0.20
langchain>=0.1.0

# YouTube API for channel monitoring
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0

# NER and NLP
spacy>=3.7.0
# Note: After installing spacy, run: python -m spacy download en_core_web_sm
